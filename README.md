

## What is JSX, and why is it used?
Answer: JSX is a syntax extension for JavaScript that allows to write code in JavaScript file

## What is the difference between State and Props?
Answer: State manages internal data of a component, pros pass parent component.

## What is the useState hook, and how does it work?
Answer: useState is a adding state of react hook.

## How can you share state between components in React?
Answer: share state in a common parent component 

## How is event handling done in React?
Answer: handing events on DOM elements